# GoReleaser configuration for LineSpec
# See: https://goreleaser.com

version: 2

before:
  hooks:
    - go mod tidy
    - go generate ./...

# Stable builds (provenance only)
builds:
  - id: linespec-stable
    main: ./cmd/linespec
    binary: linespec
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}
    ignore:
      - goos: windows
        goarch: arm64
    # No build tags - this is the stable build with provenance only

  # Beta builds — identical to stable since v2.0.0; published for backward compat
  - id: linespec-beta
    main: ./cmd/linespec
    binary: linespec
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}
    ignore:
      - goos: windows
        goarch: arm64

# Archive configurations
archives:
  - id: linespec-stable
    builds:
      - linespec-stable
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE
      - PROVENANCE_RECORDS.md

  - id: linespec-beta
    builds:
      - linespec-beta
    name_template: >-
      {{ .ProjectName }}-beta_{{ .Version }}_{{ .Os }}_{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    files:
      - README.md
      - LICENSE
      - PROVENANCE_RECORDS.md
      - LINESPEC.md

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - '^style:'
      - '^refactor:'
      - Merge pull request
      - Merge branch

# GitHub Release configuration
release:
  github:
    owner: livecodelife
    name: linespec
  
  # If the tag contains 'beta', mark as prerelease
  prerelease: auto
  
  # Release notes template
  header: |
    ## LineSpec {{ .Version }}
    
    ### Quick Install
    
    **Homebrew:**
    ```bash
    brew tap livecodelife/linespec
    brew install linespec{{ if .Prerelease }}-beta{{ end }}
    ```
    
    **Go Install:**
    ```bash
    go install github.com/livecodelife/linespec/cmd/linespec@{{ .Version }}
    ```
    
  
  footer: |
    ### Documentation

    - [PROVENANCE_RECORDS.md](./PROVENANCE_RECORDS.md) - Complete provenance reference
    - [LINESPEC.md](./LINESPEC.md) - Integration testing reference
    - [README.md](./README.md) - Installation and quick start
    
    ### Full Changelog
    
    See [CHANGELOG.md](./CHANGELOG.md) for full details.

# Publish to Homebrew tap
brews:
  # Stable formula
  - name: linespec
    repository:
      owner: livecodelife
      name: homebrew-linespec
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    url_template: "https://github.com/livecodelife/linespec/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"
    directory: Formula
    homepage: "https://github.com/livecodelife/linespec"
    description: "LineSpec - Provenance Records and Integration Testing"
    license: "MIT"
    test: |
      system "#{bin}/linespec", "provenance", "--help"
    install: |
      bin.install "linespec"
    # Only publish stable builds to the main formula
    ids:
      - linespec-stable
    skip_upload: auto

  # Beta formula
  - name: linespec-beta
    repository:
      owner: livecodelife
      name: homebrew-linespec
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    url_template: "https://github.com/livecodelife/linespec/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "Brew formula update for {{ .ProjectName }}-beta version {{ .Tag }}"
    directory: Formula
    homepage: "https://github.com/livecodelife/linespec"
    description: "LineSpec - backward-compatible alias for the linespec formula (same binary since v2.0.0)"
    license: "MIT"
    test: |
      system "#{bin}/linespec", "provenance", "--help"
    install: |
      bin.install "linespec" => "linespec-beta"
    # Only publish beta builds to the beta formula
    # Explicit string "false" — GoReleaser's SkipUpload is a string type; only "true" and "auto" trigger skips
    ids:
      - linespec-beta
    skip_upload: "false"

# Disable other artifact types for now
nfpms: []
