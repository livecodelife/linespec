id: prov-2026-75710391
title: 'Test: intake dry-run probe'
status: draft
created_at: "2026-07-31"
author: 305597777+agent-loops-boxd[bot]@users.noreply.github.com
intent: |
  Verify that the intake pipeline (Job D) correctly detects a new GitHub issue and scaffolds a
  corresponding bug-type provenance record with no unintended side effects on the codebase.
  This is a disposable test probe, not a real defect: the source issue is intentionally
  temporary and states it is "safe to close" once intake detection is confirmed working.
constraints:
  - 'Intake detection must scaffold exactly one provenance record for issue #153, tagged
    type: bug with no implements field, since the issue names no parent record.'
  - Resolving this record must not modify any application source files (pkg/**, cmd/**,
    examples/**, etc.) — the probe is disposable and must leave no functional code change
    behind.
  - 'The record must pass linespec provenance lint and linespec provenance check cleanly
    with intent, constraints, and a non-empty associated_specs populated, satisfying this
    repo''s strict enforcement level.'
  - The proof artifact referenced in associated_specs must exist on disk and require no
    build or test infrastructure to verify, since there is no functional implementation behind
    this record.
  - The record must be completable (or deprecatable) without leaving residual untracked
    changes in the working tree, matching the source issue's "safe to close" instruction.
affected_scope:
  - provenance/prov-2026-75710391.yml
  - provenance/probes/issue-153-intake-probe.md
forbidden_scope: []
type: bug
extends: prov-2026-006
supersedes: ""
superseded_by: ""
related: []
sealed_at_sha: ""
associated_specs:
  - path: provenance/probes/issue-153-intake-probe.md
associated_traces: []
monitors: []
tags:
  - intake
  - probe
