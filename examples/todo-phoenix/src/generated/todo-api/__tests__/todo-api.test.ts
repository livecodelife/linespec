/**
 * Todo Api — Generated Tests
 *
 * AUTO-GENERATED by Phoenix VCS
 * Tests module structure, server health, and Phoenix traceability.
 */

import { describe, it, expect, afterAll } from 'vitest';
import { startServer } from '../server.js';

import * as createTodo from '../create-todo.js';
import * as deleteTodo from '../delete-todo.js';
import * as listTodos from '../list-todos.js';
import * as updateTodo from '../update-todo.js';

describe('Todo Api modules', () => {
  describe('Create Todo', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(createTodo._phoenix).toBeDefined();
      expect(createTodo._phoenix.name).toBe('Create Todo');
      expect(createTodo._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(createTodo).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Delete Todo', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(deleteTodo._phoenix).toBeDefined();
      expect(deleteTodo._phoenix.name).toBe('Delete Todo');
      expect(deleteTodo._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(deleteTodo).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('List Todos', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(listTodos._phoenix).toBeDefined();
      expect(listTodos._phoenix.name).toBe('List Todos');
      expect(listTodos._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(listTodos).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

  describe('Update Todo', () => {
    it('exports Phoenix traceability metadata', () => {
      expect(updateTodo._phoenix).toBeDefined();
      expect(updateTodo._phoenix.name).toBe('Update Todo');
      expect(updateTodo._phoenix.risk_tier).toBeTruthy();
    });

    it('has exported functions', () => {
      const exports = Object.keys(updateTodo).filter(k => k !== '_phoenix');
      expect(exports.length).toBeGreaterThan(0);
    });
  });

});

describe('Todo Api server', () => {
  const instance = startServer(0); // random port

  afterAll(() => new Promise<void>(resolve => instance.server.close(() => resolve())));

  it('GET /health returns 200', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/health`);
    expect(res.status).toBe(200);
    const body = await res.json() as Record<string, unknown>;
    expect(body.status).toBe('ok');
    expect(body.service).toBe('Todo Api');
  });

  it('GET /metrics returns request counts', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/metrics`);
    expect(res.status).toBe(200);
    const body = await res.json() as Record<string, unknown>;
    expect(typeof body.requests_total).toBe('number');
  });

  it('GET /modules lists all registered modules', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/modules`);
    expect(res.status).toBe(200);
    const body = await res.json() as Array<Record<string, unknown>>;
    expect(body.length).toBe(4);
  });

  it('GET /unknown returns 404', async () => {
    await instance.ready;
    const res = await fetch(`http://localhost:${instance.port}/unknown`);
    expect(res.status).toBe(404);
  });
});
