{
  "name": "todo-phoenix",
  "version": "0.1.0",
  "description": "Generated by Phoenix VCS — 1 services",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "start:todo-api": "tsc && node dist/generated/todo-api/server.js",
    "start": "tsc && node dist/generated/todo-api/server.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.8",
    "better-sqlite3": "^11.9.1",
    "hono": "^4.7.11",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0"
  }
}
